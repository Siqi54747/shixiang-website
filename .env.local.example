# Copy this file to `.env.local` and fill the values, or export the
# same variables from your shell before running `npm run sync:decks`.
# `.env.local` is gitignored.
#
# Auth model: the sync script shells out to `lark-cli` using whoever
# has done `lark-cli auth login --domain base` locally. No separate
# Lark app / tenant token needed — you just identify WHICH Base and
# table to pull from via the two vars below.

# Decks 多维表格 app_token — 从 Base URL 里抓
# e.g. https://example.feishu.cn/base/<APP_TOKEN>?table=<TABLE_ID>&view=...
LARK_BASE_APP_TOKEN=

# Decks 表的 table_id — 同上 URL 的 ?table= 参数
LARK_BASE_TABLE_ID=
